#!/bin/bash
# FinClaude 统一入口命令
# 金融级 Claude Code 工具链整合

FINCLAUDE_VERSION="1.0.0"

# 显示帮助信息
show_help() {
    cat << 'HELP'
╔════════════════════════════════════════════════════════════╗
║  FinClaude - 金融级 Claude Code 统一入口                   ║
║  版本: 1.0.0                                               ║
╚════════════════════════════════════════════════════════════╝

用法:
  fin <command> [options]

命令:
  plan <description>    规划阶段：技术调研 + 架构设计
  dev <description>     开发阶段：TDD 强制开发
  review                代码审查：简化 + 审查
  notify <message>      手动触发飞书通知
  status                显示当前状态
  doctor                诊断环境配置
  help                  显示帮助信息
  version               显示版本信息

示例:
  ┌────────────────────────────────────────────────────────┐
  │  fin plan "支付网关重构方案"                            │
  │  fin dev "实现转账功能"                                 │
  │  fin review                                            │
  │  fin notify "自定义消息"                                │
  └────────────────────────────────────────────────────────┘

环境变量:
  FINCLAUDE_HOME        FinClaude 安装路径 (默认: ~/finclaude)
  FEISHU_WEBHOOK_URL    飞书机器人 Webhook URL
  COVERAGE_THRESHOLD    测试覆盖率阈值 (默认: 80)

更多信息: https://github.com/zzpwestlife/finclaude
HELP
}

# 规划阶段命令
cmd_plan() {
    local description="$1"
    if [ -z "$description" ]; then
        echo "❌ 错误: 请提供规划描述"
        echo ""
        echo "用法: fin plan <description>"
        echo "示例: fin plan \"支付网关重构方案\""
        exit 1
    fi
    
    echo ""
    echo "╔════════════════════════════════════════════════════════════╗"
    echo "║  🎯 FinClaude 规划阶段                                     ║"
    echo "╚════════════════════════════════════════════════════════════╝"
    echo ""
    echo "📋 描述: $description"
    echo "🔧 工具: SuperClaude (/sc:research + /sc:architect)"
    echo ""
    echo "执行中..."
    echo "────────────────────────────────────────────────────────────"
    echo ""
    
    claude -p "/sc:research '$description' && /sc:architect '$description'"
}

# 开发阶段命令
cmd_dev() {
    local description="$1"
    if [ -z "$description" ]; then
        echo "❌ 错误: 请提供开发描述"
        echo ""
        echo "用法: fin dev <description>"
        echo "示例: fin dev \"实现转账功能\""
        exit 1
    fi
    
    echo ""
    echo "╔════════════════════════════════════════════════════════════╗"
    echo "║  💻 FinClaude 开发阶段                                     ║"
    echo "╚════════════════════════════════════════════════════════════╝"
    echo ""
    echo "📋 描述: $description"
    
    # 检查 Superpowers 是否安装
    if claude -p "/superpowers:write-plan --help" >/dev/null 2>&1; then
        echo "🔧 工具: Superpowers (TDD 强制: RED → GREEN → REFACTOR)"
        echo ""
        echo "执行中..."
        echo "────────────────────────────────────────────────────────────"
        echo ""
        claude -p "/superpowers:write-plan '$description' && /superpowers:execute-plan"
    else
        echo "🔧 工具: SuperClaude (/sc:implement)"
        echo "⚠️  提示: 安装 Superpowers 可启用强制 TDD 模式"
        echo ""
        echo "执行中..."
        echo "────────────────────────────────────────────────────────────"
        echo ""
        claude -p "/sc:implement '$description'"
    fi
}

# 代码审查命令
cmd_review() {
    echo ""
    echo "╔════════════════════════════════════════════════════════════╗"
    echo "║  🔍 FinClaude 代码审查                                     ║"
    echo "╚════════════════════════════════════════════════════════════╝"
    echo ""
    
    # 运行 code-simplifier
    echo "1️⃣  代码简化..."
    echo "────────────────────────────────────────────────────────────"
    if npx code-simplifier 2>/dev/null; then
        echo ""
        echo "✅ 代码简化完成"
    else
        echo ""
        echo "⚠️  code-simplifier 未安装或运行失败"
        echo "   安装命令: /plugin install code-simplifier"
    fi
    
    echo ""
    echo "2️⃣  SuperClaude 代码审查..."
    echo "────────────────────────────────────────────────────────────"
    echo ""
    claude -p "/sc:review"
}

# 通知命令
cmd_notify() {
    local message="$1"
    if [ -z "$message" ]; then
        message="FinClaude 任务完成"
    fi
    
    echo ""
    echo "📱 发送通知: $message"
    echo ""
    
    if [ -f ~/claude-code-notification/notify-system.js ]; then
        node ~/claude-code-notification/notify-system.js --message "$message"
    else
        echo "❌ 通知系统未安装"
        echo "   安装命令: git clone https://github.com/zzpwestlife/claude-code-notification.git ~/claude-code-notification"
        exit 1
    fi
}

# 状态命令
cmd_status() {
    echo ""
    echo "╔════════════════════════════════════════════════════════════╗"
    echo "║  📊 FinClaude 状态                                         ║"
    echo "╚════════════════════════════════════════════════════════════╝"
    echo ""
    
    echo "📁 路径信息:"
    echo "   安装路径: ${FINCLAUDE_HOME:-~/finclaude}"
    echo "   通知系统: ~/claude-code-notification"
    echo ""
    
    echo "✅ 组件状态:"
    
    # 检查各组件
    if command -v ccstatusline >/dev/null 2>&1; then
        echo "   ✅ ccstatusline ($(ccstatusline --version 2>/dev/null || echo '已安装'))"
    else
        echo "   ❌ ccstatusline (npm install -g ccstatusline)"
    fi
    
    if command -v superclaude >/dev/null 2>&1; then
        echo "   ✅ SuperClaude ($(superclaude --version 2>/dev/null || echo '已安装'))"
    else
        echo "   ⚠️  SuperClaude (pipx install superclaude)"
    fi
    
    if command -v claude >/dev/null 2>&1; then
        echo "   ✅ Claude Code (已安装)"
    else
        echo "   ❌ Claude Code (npm install -g @anthropic-ai/claude-code)"
    fi
    
    if [ -f ~/claude-code-notification/notify-system.js ]; then
        echo "   ✅ 通知系统 (已安装)"
    else
        echo "   ❌ 通知系统 (未安装)"
    fi
    
    echo ""
    echo "📄 配置文件:"
    [ -f ~/.claude/settings.json ] && echo "   ✅ ~/.claude/settings.json" || echo "   ❌ ~/.claude/settings.json"
    [ -f ~/.finclaude/.env ] && echo "   ✅ ~/.finclaude/.env" || echo "   ❌ ~/.finclaude/.env"
    
    if [ -f ~/.finclaude/.env ]; then
        if grep -q "FEISHU_WEBHOOK_URL" ~/.finclaude/.env 2>/dev/null; then
            echo "   ✅ 飞书 Webhook (已配置)"
        else
            echo "   ⚠️  飞书 Webhook (未配置)"
        fi
    fi
    
    echo ""
}

# 更新命令
cmd_update() {
    echo ""
    echo "╔════════════════════════════════════════════════════════════╗"
    echo "║  🔄 FinClaude 组件更新                                     ║"
    echo "╚════════════════════════════════════════════════════════════╝"
    echo ""

    # 1. 更新 FinClaude 自身
    echo "1️⃣  更新 FinClaude..."
    echo "────────────────────────────────────────────────────────────"
    if [ -d "$FINCLAUDE_HOME" ] && [ -d "$FINCLAUDE_HOME/.git" ]; then
        cd "$FINCLAUDE_HOME"
        if git pull; then
            echo "✅ FinClaude 更新成功"
        else
            echo "⚠️  FinClaude 更新失败 (请检查 git 状态)"
        fi
    else
        echo "⚠️  跳过 (非 git 仓库)"
    fi
    echo ""

    # 2. 更新 ccstatusline
    echo "2️⃣  更新 ccstatusline..."
    echo "────────────────────────────────────────────────────────────"
    if command -v ccstatusline >/dev/null 2>&1; then
        npm update -g ccstatusline
        echo "✅ ccstatusline 更新完成"
    else
        echo "⚠️  ccstatusline 未安装，跳过"
    fi
    echo ""

    # 3. 更新 SuperClaude
    echo "3️⃣  更新 SuperClaude..."
    echo "────────────────────────────────────────────────────────────"
    if command -v superclaude >/dev/null 2>&1; then
        if command -v pipx >/dev/null 2>&1; then
            pipx upgrade superclaude
            echo "✅ SuperClaude 更新完成"
        else
            echo "⚠️  pipx 未找到，请手动更新 SuperClaude"
        fi
    else
        echo "⚠️  SuperClaude 未安装，跳过"
    fi
    echo ""

    # 4. 更新通知系统
    echo "4️⃣  更新通知系统..."
    echo "────────────────────────────────────────────────────────────"
    if [ -d ~/claude-code-notification ]; then
        cd ~/claude-code-notification
        if git pull; then
            npm install
            echo "✅ 通知系统更新成功"
        else
            echo "⚠️  通知系统更新失败"
        fi
    else
        echo "⚠️  通知系统未安装，跳过"
    fi
    echo ""

    # 5. 提示更新 MCP 插件
    echo "5️⃣  MCP 插件更新提醒..."
    echo "────────────────────────────────────────────────────────────"
    echo "MCP 插件 (superpowers, code-simplifier) 需要在 Claude Code 中手动更新："
    echo ""
    echo "👉 请在 Claude Code 中运行："
    echo "   /plugin install superpowers@superpowers-marketplace"
    echo "   /plugin install code-simplifier"
    echo ""
    
    echo "════════════════════════════════════════════════════════════"
    echo "🎉 更新流程结束"
    echo "════════════════════════════════════════════════════════════"
    echo ""
}

# 诊断命令
cmd_doctor() {
    echo ""
    echo "╔════════════════════════════════════════════════════════════╗"
    echo "║  🔧 FinClaude 环境诊断                                     ║"
    echo "╚════════════════════════════════════════════════════════════╝"
    echo ""
    
    local issues=0
    
    # 检查 Node.js
    echo "📦 运行时环境:"
    if command -v node >/dev/null 2>&1; then
        NODE_VERSION=$(node --version)
        NODE_MAJOR=$(echo $NODE_VERSION | cut -d'v' -f2 | cut -d'.' -f1)
        if [ "$NODE_MAJOR" -ge 18 ]; then
            echo "   ✅ Node.js: $NODE_VERSION"
        else
            echo "   ⚠️  Node.js: $NODE_VERSION (建议 >= 18)"
            ((issues++))
        fi
    else
        echo "   ❌ Node.js: 未安装"
        echo "      安装: https://nodejs.org/"
        ((issues++))
    fi
    
    # 检查 Claude Code
    echo ""
    echo "🤖 Claude Code:"
    if command -v claude >/dev/null 2>&1; then
        echo "   ✅ 已安装"
    else
        echo "   ❌ 未安装"
        echo "      安装: npm install -g @anthropic-ai/claude-code"
        ((issues++))
    fi
    
    # 检查 ccstatusline
    echo ""
    echo "📊 状态监控:"
    if command -v ccstatusline >/dev/null 2>&1; then
        echo "   ✅ ccstatusline 已安装"
    else
        echo "   ❌ ccstatusline 未安装"
        echo "      安装: npm install -g ccstatusline"
        ((issues++))
    fi
    
    # 检查通知系统
    echo ""
    echo "📱 通知系统:"
    if [ -f ~/claude-code-notification/notify-system.js ]; then
        echo "   ✅ 已安装"
        
        # 检查 .env
        # 优先检查全局配置 ~/.finclaude/.env
        if [ -f ~/.finclaude/.env ]; then
            if grep -q "FEISHU_WEBHOOK_URL" ~/.finclaude/.env 2>/dev/null; then
                 # 检查是否是默认值
                 if grep -q "hook/xxxxxx" ~/.finclaude/.env; then
                     echo "   ⚠️  飞书配置: 请修改默认 Webhook URL"
                     echo "      文件位置: ~/.finclaude/.env"
                     ((issues++))
                 else
                     echo "   ✅ 飞书配置: 已配置 (~/.finclaude/.env)"
                 fi
            else
                echo "   ⚠️  飞书配置: 未找到 FEISHU_WEBHOOK_URL"
                echo "      文件位置: ~/.finclaude/.env"
                ((issues++))
            fi
        elif [ -f ~/claude-code-notification/.env ]; then
            echo "   ✅ 飞书配置: 已配置 (本地兼容模式)"
        else
            echo "   ⚠️  飞书配置: 未找到配置文件"
            echo "      请创建或编辑: ~/.finclaude/.env"
            ((issues++))
        fi
    else
        echo "   ❌ 未安装"
        echo "      安装: git clone https://github.com/zzpwestlife/claude-code-notification.git ~/claude-code-notification"
        ((issues++))
    fi
    
    # 检查配置文件
    echo ""
    echo "⚙️  配置文件:"
    if [ -f ~/.claude/settings.json ]; then
        echo "   ✅ ~/.claude/settings.json"
    else
        echo "   ❌ ~/.claude/settings.json"
        ((issues++))
    fi
    
    if [ -f ~/.finclaude/.env ]; then
        echo "   ✅ ~/.finclaude/.env"
    else
        echo "   ⚠️  ~/.finclaude/.env (可选)"
    fi
    
    # 总结
    echo ""
    echo "════════════════════════════════════════════════════════════"
    if [ $issues -eq 0 ]; then
        echo "🎉 环境检查通过！FinClaude 已就绪"
    else
        echo "⚠️  发现 $issues 个问题，请根据提示修复"
        echo ""
        echo "快速修复:"
        echo "   运行 ./install.sh 重新安装"
    fi
    echo "════════════════════════════════════════════════════════════"
    echo ""
}

# 主入口
case "${1:-}" in
    plan)
        shift
        cmd_plan "$@"
        ;;
    dev)
        shift
        cmd_dev "$@"
        ;;
    review)
        cmd_review
        ;;
    notify)
        shift
        cmd_notify "$@"
        ;;
    status)
        cmd_status
        ;;
    doctor)
        cmd_doctor
        ;;
    update)
        cmd_update
        ;;
    help|--help|-h)
        show_help
        ;;
    version|--version|-v)
        echo "FinClaude v$FINCLAUDE_VERSION"
        ;;
    "")
        show_help
        ;;
    *)
        echo "❌ 错误: 未知命令 '${1:-}'"
        echo ""
        show_help
        exit 1
        ;;
esac
